{
  "name": "@listock/server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --clear-screen=false --env-file=.env.local ./index.ts",
    "build": "tsc",
    "types": "tsc --noEmit",
    "test": "vitest --config ./vitest.config.ts"
  },
  "dependencies": {
    "@orpc/server": "^1.13.4",
    "@supabase/supabase-js": "^2.90.1",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  },
  "peerDependencies": {
    "@listock/supabase": "*",
    "@listock/tests": "*"
  }
}
