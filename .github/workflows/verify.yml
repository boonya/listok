name: Verification 👮🏽‍♂️

on:
  workflow_dispatch:
  workflow_call:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:

  biome:
    uses: ./.github/workflows/verify-biome.yml
    secrets: inherit

  types:
    uses: ./.github/workflows/verify-types.yml
    secrets: inherit

  tests:
    uses: ./.github/workflows/verify-tests.yml
    secrets: inherit

  build:
    uses: ./.github/workflows/verify-build.yml
    secrets: inherit
